interface:
  display_name: "Bootstrap Codex Project"
  short_description: "Build adaptive, trustworthy Codex project context"
  default_prompt: "Use $bootstrap-codex-project to turn my project idea or repository into the smallest trustworthy Codex context system, adding long-project planning controls only when the evidence requires them."
