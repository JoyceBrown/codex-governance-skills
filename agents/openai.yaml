interface:
  display_name: "Bootstrap Codex Project"
  short_description: "Build adaptive Codex context that improves from evidence"
  default_prompt: "Use $bootstrap-codex-project to build or repair the smallest trustworthy Codex context system. Apply only reviewed experience that matches this project, and capture reusable lessons safely when real friction appears."
