---
name: architecture-health
description: 审查软件或自动化系统的边界、依赖、接口、状态、容量、演进和回滚健康度；只在证据支持时提出结构性改动。
---

# Architecture Health

架构健康不是“看起来整洁”，而是系统在真实变化、失败和恢复路径下仍有清楚的所有权和可验证行为。

## 检查面

- 模块、数据、配置、Provider、工作台和用户路径的边界。
- 调用方、接口契约、状态转换、缓存和失败传播。
- 单一事实源、重复状态、隐式耦合、版本/权限/供应链边界。
- 性能、资源增长、并发、可观测性、迁移、回滚和长期维护。

## 工作方式

先建立当前快照和覆盖范围，再区分 `Observed`、`Supported`、`Open`、`Contested`。对每个重大建议写出收益、代价、替代方案、逆转条件和最小验证；不要因为架构问题听起来重要就扩大范围。

默认只读。需要实现时把结论交给 Bootstrap 的计划权责、Guard 的门禁和 TDD Loop 的实现闭环。不要新增数据库、常驻 Broker、全局记忆或复杂代理层来掩盖缺少证据。

