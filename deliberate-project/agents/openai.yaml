interface:
  display_name: "项目审议分析"
  short_description: "仅显式调用的多角度项目审议，结合动态审法与证据比对"
  default_prompt: "使用 $deliberate-project 根据我的提示词、目标、要求与约束自动判断审理侧重点，再从多种角度、推导方式和证据路径分析当前项目；保留竞争判断，仅在我明确要求时定案。"

policy:
  allow_implicit_invocation: true
